# Educational Data Dashboard

Полнофункциональный веб-дашборд для визуализации учебных данных с возможностью загрузки CSV файлов, отображения графиков, таблиц и фильтрации данных.

**Проект разработан в рамках практики в Университете «Синергия», Кафедра Цифровой экономики.**

## 🚀 Технологии

### Backend
- **Node.js** + **Express** - серверная часть
- **Multer** - загрузка файлов
- **csv-parser** - парсинг CSV файлов
- **CORS** - настройка CORS для работы с фронтендом

### Frontend
- **React** + **Vite** - клиентская часть
- **Chart.js** + **react-chartjs-2** - визуализация данных
- **Axios** - HTTP запросы
- **Tailwind CSS** - стилизация

## 📁 Структура проекта

```
.
├── backend/          # Node.js + Express сервер
│   ├── server.js     # Основной файл сервера
│   ├── package.json  # Зависимости backend
│   └── Dockerfile    # Docker конфигурация для деплоя
├── frontend/         # React (Vite) приложение
│   ├── src/
│   │   ├── components/
│   │   │   ├── FileUploader.jsx  # Компонент загрузки CSV
│   │   │   ├── DataTable.jsx     # Таблица с данными
│   │   │   ├── Dashboard.jsx     # Графики и визуализация
│   │   │   ├── MetricsCards.jsx  # Карточки с метриками
│   │   │   └── Filters.jsx       # Фильтры данных
│   │   ├── App.jsx   # Главный компонент
│   │   └── main.jsx  # Точка входа
│   ├── package.json  # Зависимости frontend
│   └── vite.config.js # Конфигурация Vite
└── README.md         # Документация
```

## 🛠️ Установка и запуск

### Предварительные требования
- Node.js (версия 18 или выше)
- npm или yarn

### Backend

1. Перейдите в папку backend:
```bash
cd backend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите сервер:
```bash
npm run dev
```

Сервер будет доступен по адресу: `http://localhost:3001`

### Frontend

1. Перейдите в папку frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите приложение:
```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

## 📊 Функциональность

### Backend API

#### `GET /api/sample-data`
Возвращает сгенерированные тестовые данные.

**Параметры запроса:**
- `count` (опционально) - количество записей (по умолчанию 100)

**Пример:**
```bash
GET http://localhost:3001/api/sample-data?count=50
```

#### `POST /api/upload`
Принимает CSV файл и возвращает распарсенные данные в формате JSON.

**Формат CSV файла:**
```csv
id,student,course,date,activity_type,score,time_min
1,Иванов Иван,Математика,2025-12-20,Лекция,85,60
2,Петров Петр,Физика,2025-12-21,Практика,90,45
```

**Пример запроса:**
```bash
curl -X POST http://localhost:3001/api/upload \
  -F "file=@data.csv"
```

#### `GET /api/health`
Проверка работоспособности сервера.

### Frontend компоненты

- **FileUploader** - загрузка CSV файлов с валидацией
- **MetricsCards** - отображение ключевых метрик (количество записей, средний балл, среднее время, количество студентов)
- **Filters** - фильтрация данных по курсу и типу активности
- **Dashboard** - три типа графиков:
  - **Bar Chart**: средний балл по курсам
  - **Line Chart**: активность по дням (последние 30 дней)
  - **Pie Chart**: распределение по типам активностей
- **DataTable** - таблица данных с пагинацией и сортировкой

## 🎨 Особенности

- ✅ Адаптивный дизайн (работает на мобильных устройствах)
- ✅ Обработка ошибок при загрузке файлов
- ✅ Валидация CSV файлов
- ✅ Сортировка данных в таблице
- ✅ Пагинация таблицы (10 записей на страницу)
- ✅ Фильтрация данных на клиенте
- ✅ Красивая визуализация с Chart.js
- ✅ Минималистичный и современный UI

## 🚢 Деплой

### Backend на Render

1. Создайте аккаунт на [Render.com](https://render.com)
2. Подключите ваш репозиторий
3. Выберите тип сервиса "Web Service"
4. Укажите:
   - **Root Directory**: `backend`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Environment**: Node
   - **Node Version**: 18
5. Добавьте переменную окружения `PORT` (Render автоматически установит порт)

Альтернативно, можно использовать Dockerfile:
- Render автоматически обнаружит Dockerfile в папке `backend`
- Используйте команду: `docker build -t dashboard-backend ./backend`
- Запустите контейнер с портом 3001

### Frontend на Vercel

1. Установите Vercel CLI:
```bash
npm install -g vercel
```

2. Перейдите в папку frontend:
```bash
cd frontend
```

3. Создайте файл `.env.production`:
```env
VITE_API_URL=https://your-backend-url.onrender.com
```

4. Соберите проект:
```bash
npm run build
```

5. Деплой через Vercel CLI:
```bash
vercel
```

Или через веб-интерфейс:
1. Зайдите на [Vercel.com](https://vercel.com)
2. Подключите ваш репозиторий
3. Укажите:
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`
4. Добавьте переменную окружения `VITE_API_URL` с URL вашего backend сервера

## 📝 Формат данных

Каждая запись должна содержать следующие поля:

- `id` - уникальный идентификатор (число)
- `student` - имя студента (строка)
- `course` - название курса (строка)
- `date` - дата в формате YYYY-MM-DD (строка)
- `activity_type` - тип активности (строка)
- `score` - балл (число от 0 до 100)
- `time_min` - время в минутах (число)

## 🐛 Решение проблем

### Backend не запускается
- Убедитесь, что порт 3001 свободен
- Проверьте, что все зависимости установлены (`npm install`)
- Проверьте версию Node.js (должна быть 18+)

### Frontend не подключается к Backend
- Убедитесь, что backend запущен на порту 3001
- Проверьте настройки CORS в backend
- Проверьте переменную окружения `VITE_API_URL`

### Ошибки при загрузке CSV
- Убедитесь, что файл имеет расширение `.csv`
- Проверьте формат данных в CSV файле
- Максимальный размер файла: 10MB

## 📄 Лицензия

MIT

## 👤 Автор

Создано для визуализации учебных данных
